# Copy to .env.local for local dev
# Set in Vercel dashboard for production

# FastAPI backend URL (Railway URL after deploy)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Admin email (must match the backend ADMIN_EMAIL). Only gates the admin UI;
# the backend independently enforces admin access.
NEXT_PUBLIC_ADMIN_EMAIL=akshatgupta428@gmail.com
